kind: pipeline
type: docker
name: default

steps:
- name: publish
  image: gitea.7willows.com/7willows/node-builder:latest
  commands:
    - npm install
    - npm publish
  # when:
  #  event:
  #    tag

image_pull_secrets:
- dockerconfig
